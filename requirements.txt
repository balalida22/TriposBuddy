# Direct dependencies — pip resolves compatible versions on the target platform.
# Local dev: uv pip install -r requirements.txt
# SRCF deployment: pip install -r requirements.txt

email-validator>=2.0
flask>=3.0,<4
flask-bcrypt>=1.0
flask-login>=0.6
flask-migrate>=4.0,<5
flask-sqlalchemy>=3.1,<4
flask-wtf>=1.2
gunicorn>=22
psycopg2-binary>=2.9
python-dotenv>=1.0
wtforms>=3.1
